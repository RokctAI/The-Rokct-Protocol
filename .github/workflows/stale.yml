name: Stale
on:
  schedule:
    - cron: '30 1 * * *'

jobs:
  stale:
    uses: RokctAI/shared-workflows/.github/workflows/universal-stale.yml@1d5ebbcdf3dc771fed587fe90e5f91f3a305392c # shared-workflows main @ 2026-08-24
    permissions: write-all
    secrets:
      COUNTER_API_KEY: ${{ secrets.COUNTER_API_KEY }}
