name: Size Labeler
on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  size:
    uses: RokctAI/shared-workflows/.github/workflows/universal-size.yml@c9acb66a7449b7ecaef6c909fbf8b0f899464ac5 # shared-workflows main @ 2026-08-22
    permissions: write-all
    secrets:
      COUNTER_API_KEY: ${{ secrets.COUNTER_API_KEY }}
