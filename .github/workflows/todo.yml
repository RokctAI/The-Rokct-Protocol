name: ToDo Bot
on:
  push:
    branches: [main]

jobs:
  todo:
    uses: RokctAI/shared-workflows/.github/workflows/universal-todo.yml@1d5ebbcdf3dc771fed587fe90e5f91f3a305392c # shared-workflows main @ 2026-08-24
    permissions: write-all
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
      COUNTER_API_KEY: ${{ secrets.COUNTER_API_KEY }}
