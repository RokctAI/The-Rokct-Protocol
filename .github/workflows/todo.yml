name: ToDo Bot
on:
  push:
    branches: [main]

jobs:
  todo:
    uses: RokctAI/shared-workflows/.github/workflows/universal-todo.yml@e6e29abcfeb0282f0be44ab21fb16d4f92957e6e # shared-workflows main @ 2026-08-15
    permissions: write-all
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
      COUNTER_API_KEY: ${{ secrets.COUNTER_API_KEY }}
