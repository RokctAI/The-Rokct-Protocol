name: ToDo Bot
on:
  push:
    branches: [main]

jobs:
  todo:
    uses: RokctAI/shared-workflows/.github/workflows/universal-todo.yml@97d7d6f3a7ed312da4c651be55ea95774c190a89 # shared-workflows main @ 2026-08-16
    permissions: write-all
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
      COUNTER_API_KEY: ${{ secrets.COUNTER_API_KEY }}
