name: ToDo Bot
on:
  push:
    branches: [main]

jobs:
  todo:
    uses: RokctAI/shared-workflows/.github/workflows/universal-todo.yml@3ad81fe42b9302f6601510bb8902aab8fa7613e7 # shared-workflows main @ 2026-08-22
    permissions: write-all
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
      COUNTER_API_KEY: ${{ secrets.COUNTER_API_KEY }}
