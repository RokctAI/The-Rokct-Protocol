name: ToDo Bot
on:
  push:
    branches: [main]

jobs:
  todo:
    uses: RokctAI/shared-workflows/.github/workflows/universal-todo.yml@c9acb66a7449b7ecaef6c909fbf8b0f899464ac5 # shared-workflows main @ 2026-08-22
    permissions: write-all
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
      COUNTER_API_KEY: ${{ secrets.COUNTER_API_KEY }}
