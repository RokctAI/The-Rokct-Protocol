# Opportunities Registry Skill (.rok)

This is a RokctAI-only skill populated under the `.rok` directory. It exposes thin delegating callers that dynamically sync and invoke the full script implementations from `core/utils/opportunities/`.
