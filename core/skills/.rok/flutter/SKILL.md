---
name: flutter-composer
description: Orchestrates Flutter/Dart SDK module composition.
---
# Flutter Composer Skill

This skill automates the compilation and wiring of reusable Dart SDKs into target Flutter App Shells.

## Usage
Run the script `scripts/compose.py` inside the root of any Flutter App Shell containing a valid `composer.json` configuration file.
